/**
 * Scope object used for member completion (e.g. console.log, Math.floor).
 * Uses real browser globals so methods/properties are enumerated automatically.
 */
export const playgroundScope: Record<string, unknown> = {
  console,
  Math,
  JSON,
  Array,
  Object,
  String,
  Number,
  Boolean,
  Date,
  RegExp,
  Map,
  Set,
  Promise,
  Symbol,
  BigInt,
  Error,
  TypeError,
  ReferenceError,
  SyntaxError,
  RangeError,
  parseInt,
  parseFloat,
  isNaN,
  isFinite,
  encodeURI,
  decodeURI,
  encodeURIComponent,
  decodeURIComponent,
  setTimeout,
  setInterval,
  clearTimeout,
  clearInterval,
  globalThis,
};
